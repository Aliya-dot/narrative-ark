<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cloud model endpoints remain HTTPS-only in application validation.
         Cleartext is enabled at the OS layer only so the scoped Tauri HTTP
         capability can reach user-selected RFC1918 Ollama hosts. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
